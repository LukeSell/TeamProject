package cafe;

/**
 * Tests for the order class.
 * 
 * <p>
 * These tests initially were developed to test the initial implementations of
 * methods, however after further development these tests became redundant,
 * hence why are commented.
 * </p>
 *
 */

public class TestOrder {
  
  /*
   * Order.txt orderlist; Food foods; Table tables;
   * 
   * @Before public void setUp() { orderlist = new Order.txt(); foods = new
   * Food("soup", "test", null, 19); tables = new Table(2, 4); }
   */
  
  /*
   * 
   * @Test public void test_empty() { assertTrue(orderlist.emptyList()); }
   * 
   * @Test public void test_addlist() { Order.txt order1 = new Order.txt(foods,
   * tables); orderlist.addList(order1); assertFalse(orderlist.emptyList()); }
   * 
   * @Test public void test_poplist() { Order.txt order2 = new Order.txt(foods,
   * tables); orderlist.addList(order2); orderlist.popList();
   * assertTrue(orderlist.emptyList()); }
   * 
   * @Test public void test_toplist() { Order.txt order3 = new Order.txt(foods,
   * tables); orderlist.addList(order3); //assertEquals(orderlist.topList() =
   * order3); should check order values which are foods and tables }
   */
}
